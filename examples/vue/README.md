# Vue Example

This app demonstrates `@xcvzmoon/vue-cropperjs` in a plain Vue 3 + Vite project.

It shows:

- `CropperImage` in a regular Vue app
- `v-model:data` crop state syncing
- source image switching
- exporting the selection to a preview image

## Run

```sh
pnpm install
pnpm dev
```
